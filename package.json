{
  "name": "kuma-space",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "postinstall": "bunx prisma generate --schema=apps/kuma-elysia/prisma/schema.prisma",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "prettier": "^3.4.1",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "bun@1.1.34",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
